{{> header 'home'}}
<div class="container-fluid">
<header>
	<div class="contents">
{{> cartInfo}}
{{> pageHeader}}
{{> navbar}}
	</div>
</header>
<div class="row albums mmt">
	<!--h3>New Releases</h3-->
	{{> filters}}
	<div class="releaseItems">
	{{>items albuns }}
	</div>
</div>
</div>
{{> footer}}
